#include <ObjectManager/ObjectManager.h>

int main(int argc, char *argv[])
{ 
    //implementation depends on which approach is chosen
    //
   //in the case of a library, this function is not needed
    return 0;
}
